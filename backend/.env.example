# Entorn: development | production
APP_ENV=development

# Secret JWT (mínim 32 caràcters en producció)
JWT_SECRET_KEY=canvia_aixo_per_un_secret_molt_llarg_i_random

# Expiració token (segons)
JWT_ACCESS_TOKEN_EXPIRES=3600

# CORS (separat per comes). Obligatori en producció.
# Exemple: https://tudomini.com,https://app.tudomini.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Opció 1: connexió completa
# DATABASE_URL=postgresql://usuari:password@host:5432/nom_bd

# Opció 2: variables separades
DB_HOST=localhost
DB_NAME=tfg_senderisme
DB_USER=postgres
DB_PASSWORD=canvia_aixo
DB_PORT=5432
